# Lightweight, CPU-only dependency set used by GitHub Actions.
# Excludes unsloth/bitsandbytes/torch-heavy packages, which require a GPU
# runtime and are not exercised by the unit test suite (training code paths
# lazily import them and are covered separately via manual/GPU CI runs).
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
pyyaml>=6.0.1
numpy>=1.26.0
transformers>=4.42.0
datasets>=2.20.0

pytest>=8.2.0
pytest-cov>=5.0.0
httpx>=0.27.0
ruff>=0.5.0
mypy>=1.10.0